// =========================================================================
// DEPRECATED — Cloudflare Pages legacy. Not in use on GitHub Pages.
// =========================================================================
// This file was a Cloudflare Pages Function that served Supabase config
// from environment variables at /functions/supabase-config.js. After the
// migration to GitHub Pages (May 2026), Pages does not execute these
// functions — the file is inert and not publicly served because it lives
// outside /cloudflare-deploy/ (the GitHub Pages publish path).
//
// Configuration is now hardcoded in /cloudflare-deploy/supabase-init.js
// (publishable key only — RLS-bound, safe to ship in client).
//
// SAFE TO DELETE. To remove from repo root:
//   git rm -r functions/
//   git commit -m "Remove dead Cloudflare functions/ dir (GH02)"
// =========================================================================
